
                 TASKMAN TASK SCHEDULER - INSTALL INFORMATION

                               Version 1.05(31)
                             2015-05-31T20:15:38Z

                    Copyright 2014-2015 Spinlock Solutions
                http://techpubs.spinlocksolutions.com/taskman/



Table of Contents:

1. Prerequisites
1.1. Packages
1.2. Ruby and Ruby GEMs
1.3. STFL
1.4. MySQL
1.5. Taskman Database Configuration
2. Running Taskman


-----------------------------------------------------------------------------

1. PREREQUISITES

To run Taskman, you need some generic packages, Ruby >= 1.9, a couple Ruby
GEMs, STFL and MySQL.

1.1. Packages:
--------------

sudo apt-get install swig mysql-server libmysqlclient-dev

1.2. Ruby and Ruby GEMs:
------------------------

sudo gem install gettext activerecord mysql2 ruby-terminfo

1.3. STFL:
----------

STFL is a "Structured Terminal Forms Language/Library", available at
http://www.clifford.at/stfl/.

Please pay attention that ruby/stfl.so exists, which it will if 'swig' and
Ruby header/development files have been installed:

wget http://www.clifford.at/stfl/stfl-0.24.tar.gz
tar zvxf stfl-0.24.tar.gz
cd stfl-0.24
make
ls -al ruby/stfl.so # <-- Must be present
sudo make install
sudo ldconfig

1.4. MySQL:
-----------

Once MySQL server is installed, the following configuration has to be
performed:

sudo -H mysql -e 'create database taskman'
sudo -H mysql taskman < sql/taskman.sql
mysql taskman -e "GRANT ALL PRIVILEGES ON taskman.* TO taskman@'localhost'
IDENTIFIED BY 'taskman'"

This will create the database and grant permissions on the default database to
the default user and default password. Taskman has already been preconfigured
to use these defaults.

If you want to change these defaults, please do so and then read the following
section.

1.5. Taskman Database Configuration:
------------------------------------

By default, Taskman connects to the local MySQL database 'taskman' as user
'taskman' with the password 'taskman'.

It is suggested to change these values and then create or modify Taskman
configuration to use the correct connection information.

The default configuration file is ~/.taskman/taskmanrc. Please create it with
the following content (and with adjusting any values as necessary):

--conn "main:[adapter:'mysql2',host:'localhost',username:'taskman',
password:'taskman',database:'taskman']"

** NOTE: The above should all be on a single line. Also, quotes must remain
as-is and there must be no any whitespace in the quoted string!

-----------------------------------------------------------------------------

2. RUNNING TASKMAN

When all the prerequisites have been installed and the MySQL configuration
data is in place, Taskman can be tested with:

taskman -h

Then, a default GUI can be tested:

taskman

Then, a 256-color theme can be tested:

taskman --theme desert --colors 256

** NOTE: Keys like HOME/END/PGUP/PGDOWN seem to not work if --colors 256 is
specified. (Option --colors changes the terminal's TERM variable,
inadvertently also affecting the keys.)

Enjoy!

 
